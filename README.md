# journalwatch

A minimal tool which forwards journald logs to AWS cloudwatch.

## Usage

```bash
journalwatch --
```

See all flags:

```bash
journalwatch --help
```

```text

```

Example systemd service

```unit file (systemd)

```
